import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Resume",
  description: "Pallab's Resume",
};

const ResumePage = () => {
  return (
    <div>
      <h1>This is ResumePage Component</h1>
    </div>
  );
};

export default ResumePage;
